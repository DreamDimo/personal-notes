# 面试总结

## 12月25日 上海什风子印科技

### 1. 当一个 html 非常长的时候，超过了模型agent限制，应该怎么处理

好像说什么展开，类似于代码的那种展开，就是可以隐藏可以展开，但是怎么展开

### 2. 在进行tool calling 的时候如果我们模型给的输入是长结构化参数，往往质量会很差，是什么原因呢？应该怎么解决

就是一个现象，进行工具调用的时候，如果输入的json信息非常长，那一般这个json格式有问题，为什么，怎么处理

### 3. 对于 agent 我们如果想要流式输出，除了 tool calling 以外还有什么方法可以stream out

tool calling 是怎么实现流式输出的？

还有什么方法？

就比如cursor代码那里是流式输出

### 4. 对于一个上下文非常长的信息，除了压缩和RAG还有什么解决办法

对于claude code它是压缩前面的历史信息，保存现在的历史信息

### 5. CDP和playwright区别

### 6. 浏览器指纹，User-agent参数

### 7. tool calling 里面的json schema 可以写哪些type

就是 tool calling 的返回值格式有哪些

### 8. MCP 和 tool calling 的区别是什么

### 9. 怎么去兼容不同模型的接口，比如OPENAI的格式

### 10. 怎么去实现 aop

### 11. async 和 sync 区别

### 12. 推荐阅读 AgentQL

